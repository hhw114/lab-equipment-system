<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hhw.mapper.BorrowRecordMapper">

    <select id="selectBorrowedRecordByEquipmentId"
            resultType="com.hhw.domain.po.BorrowRecord">
        SELECT *
        FROM borrow_record
        WHERE equipment_id = #{equipmentId}
          AND status = 'BORROWED'
        LIMIT 1
    </select>

    <select id="selectDailyTrend" resultType="com.hhw.domain.vo.DailyBorrowStat">
        SELECT
        DATE(borrow_time) AS date,
        COUNT(*) AS count
        FROM borrow_record
        WHERE borrow_time BETWEEN #{startTime} AND #{endTime}
        AND status IN
        <foreach collection="statusList" item="status" open="(" separator="," close=")">
            #{status}
        </foreach>
        GROUP BY DATE(borrow_time)
        ORDER BY date ASC
    </select>
</mapper>
